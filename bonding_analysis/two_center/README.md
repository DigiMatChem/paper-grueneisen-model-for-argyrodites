# Descriptions of scripts

- To visualize the Local environments detected using bond strengths as criterion use the `app.py` script. Please use `python app.py --help` for details on its usage. Hovering over the bonds in the app will show corresponding neighbor *ICOHPs* or *ICOBIs* depending on the file being read.